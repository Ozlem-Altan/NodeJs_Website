<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Urunler</h1>

    <ul>
        <% products.forEach(product => { %>
            <% if (product.price >25000 ) { %> <!-- şeklinde şarta bağlı listeleme yapılabilir (&& product.isActive)-->
             
                <li>
                    <a href="/products/<%= product.id %>">
                        <%= product.name %> - <%= product.price %>
                    </a>
                </li> <!-- js den dönderdiğimiz ürünleri burada ejsout simgeleri ile dönerek ekana yazdırdık-->
            <% } %>
        <% }) %>
    </ul>
</body>
</html>